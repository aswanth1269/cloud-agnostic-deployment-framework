apiVersion: v1
kind: Service
metadata:
  name: cloud-agnostic-demo-service
  labels:
    app: cloud-agnostic-demo
spec:
  type: ClusterIP
  selector:
    app: cloud-agnostic-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
